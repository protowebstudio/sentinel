/**
 * Sentinel Telemetry — DISABLED
 *
 * Sentinel is an institutional presentation layer and must remain read-only.
 * No tokens. No mutation. No ingestion.
 */
export async function sendTelemetry() {
  return;
}
